<%= link_to "" , 
    ownership_path(current_user.wants.find_by(:item_id => item_id) , 
      item_id: item_id , type: "Want") ,
    :class => "want-#{item_id} btn-want btn btn-danger btn-sm wanted",
    :remote => true,
    :method => :delete %>