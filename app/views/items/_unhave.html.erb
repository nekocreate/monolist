<%= link_to "" , 
  ownership_path( current_user.haves.find_by(:item_id => item_id) , 
    item_id: item_id , type: "Have") ,
  :class => "have-#{item_id} btn-have btn btn-warning btn-sm haved",
  :remote => true,
  :method => :delete %> 